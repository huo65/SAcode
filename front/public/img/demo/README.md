This directory contains classroom-demo image assets referenced by
`backend/DB-market/src/main/resources/schema.sql`.

Keep demo data image URLs under `/img/demo/...` so a fresh checkout can
render seeded products and restaurants without relying on runtime uploads.
