# 前后端联调验收报告

## 后端补充

| 验证项 | 状态 | 说明 |
| --- | --- | --- |
| 商家评价列表 `GET /review/merchant/list` | 通过 | 返回 `list/overview/ratingDistribution`，商家账号验证成功 |
| 用户评价列表 `GET /review/customer/list` | 通过 | 返回 `list/reviews/total`，用户账号验证成功 |
| 评价回复字段兼容 `POST /review/reply` | 通过 | 后端兼容 `reviewId`、`replyContent`、`reply` |
| 订单查询参数兼容 `usrId/userId` | 通过 | 后端测试覆盖，非管理员仍以 JWT 为准 |
| 管理端钱包流水 `type=all` | 通过 | `GET /ops/wallet/transactions?type=all&page=1&pageSize=20` 返回成功 |
| 管理端审计日志 `actionType=all` | 通过 | 返回 `auditLogs/list/total/stats/page/pageSize` |

## 前端适配

| 验证项 | 状态 | 说明 |
| --- | --- | --- |
| 管理端钱包流水真实 payload 适配 | 通过 | 已兼容 `transactions/list` 与 `relatedOrderId/remark/createdTime` |
| 管理端审计日志真实 payload 适配 | 通过 | 已兼容 `list/auditLogs/total/stats` |
| 商家评价页真实数据适配 | 通过 | 已消费后端 `list/overview/ratingDistribution` |
| 商家回复提交参数 | 通过 | 已改为 `reviewId + replyContent` |
| `fetch` 解包误判排查 | 通过 | 页面中旧式 `data.code === 200` 已清理，仅图片上传 helper 保留 axios 原始响应判断 |

## 自动化验证

| 验证项 | 状态 | 结果 |
| --- | --- | --- |
| 后端单元测试 | 通过 | 45 个测试，0 失败 |
| 前端构建 | 通过 | `npm run build` 成功 |
| 前端单元测试 | 通过 | 3 个测试，0 失败 |
| 前端服务入口 | 通过 | `http://localhost:5173/` 返回 Vite 页面 |
| 后端服务入口 | 通过 | `POST /user/login` 登录成功 |
| 浏览器逐页自动验收 | 待继续 | Headless Edge 启动被自动审批额度限制拒绝，未能完成自动截图/DOM 巡检 |

## 已验证接口样例

| 接口 | 状态 | 返回结构 |
| --- | --- | --- |
| `/ops/wallet/transactions?type=all&page=1&pageSize=20` | 通过 | `transactions/list/total/page/pageSize/platformStats/typeDistribution` |
| `/ops/audit/list?actionType=all&page=1&pageSize=20` | 通过 | `auditLogs/list/total/stats/page/pageSize` |
| `/review/merchant/list?replyStatus=all` | 通过 | `list/overview/ratingDistribution` |
| `/review/customer/list` | 通过 | `list/reviews/total` |

## 待人工或授权继续验收

- 管理端：dashboard、wallet、audit、goods、order、category、after-sale、user、ops。
- 商家端：dashboard、goods、orders、after-sale、store、ops、review、info。
- 用户端：home、restaurants、cart、orders、wallet、address、review、after-sale、info。
- 骑手端：overview、available、delivering、history、earnings、info。

当前阻塞仅为浏览器自动化权限，不是构建、接口或服务启动问题。
